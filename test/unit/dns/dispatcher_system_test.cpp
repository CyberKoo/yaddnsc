//
// ResolverDispatcher unit tests — legacy system backend (libresolv) [DEPRECATED].
//
// Compiled with the legacy dispatcher_system.cpp (will be removed before 1.0.0).
// See test/fixtures/dispatcher_tests.h for the shared test bodies.  The shared
// header branches assertions on YADDNSC_NATIVE_DISPATCHER to account for the
// legacy backend's empty-vector collapse on non-definitive failures.
// =============================================================================

#include "fixtures/dispatcher_tests.h"
