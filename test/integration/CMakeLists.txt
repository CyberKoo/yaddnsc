# ==============================================================================
# Integration tests — end-to-end workflows in a controlled I/O environment
#
# These tests exercise complete workflows (e.g. config → resolve → update)
# using real I/O on a controlled network namespace or test fixture.
#
# Currently empty — add integration test targets here when they are created.
# ==============================================================================
